/*
Package registry provides a simple forwarding mechanism that send all requests
for a source type to a specified backend. The registry is designed to look like
a backend itself, making it a simple way to mux backends together.
*/
package registry
