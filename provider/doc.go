/*
Package provider implements several Providers as specified in
github.com/jonnrb/scache/registry. Keep in mind that the registry itself is a
valid gRPC provider and `registry.Upstream`.
*/
package provider
